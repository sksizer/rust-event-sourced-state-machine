[package]
name = "serde-command"
version = "0.1.0"
edition = "2024"

[features]
tokio = ["dep:tokio"]

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
tokio = { version = "1", features = ["process"], optional = true }

[dev-dependencies]
serde_json = "1.0.149"
